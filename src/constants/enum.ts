export enum UserVerifyStatus {
  Unverified,
  Verified,
  Banned
}

export enum TokenType {
  AccesToken,
  RefreshToken,
  ForgotPasswordToken,
  verifyEmailToken
}

export enum MediaType {
  Image,
  Video
}
